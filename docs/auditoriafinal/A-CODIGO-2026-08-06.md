# AUDITORÍA DE CIERRE DE H1 · BLOQUE A — EL CÓDIGO

**2026-08-06.** Primero de tres bloques. ⛔ **Este documento no se reescribe nunca.**

> ⛔⛔ **Esta tanda NO ha arreglado nada.** El árbol de trabajo quedó sin un solo
> cambio en `src/`, `tools/` ni `.githooks/`; lo único escrito es este registro.
> Todo lo que sigue se ANOTA. Qué se arregla se decide después, con la lista
> entera delante.

---

## ⭐⭐⭐ 0 · LOS INSTRUMENTOS DE ESTA AUDITORÍA FALLARON CUATRO VECES

Va primero porque condiciona todo lo demás. *El auditor también miente*, y aquí
mintió cuatro veces antes de medir nada:

| # | el instrumento | qué hacía mal | cómo se cazó |
|---|---|---|---|
| 1 | censo de `require` v1 | contaba `require()` escritos **dentro de comentarios** | leyendo el único «no resuelve» que sacó |
| 2 | censo v2 | los seguía contando **dentro de cadenas** | ⭐ **su propio control lo tumbó**: 2 de 5 casos en rojo |
| 3 | precarga que apaga el centinela v1 | requirió `f:/…/portales.js` y el script requiere `F:\…\portales.js` ⇒ **dos entradas en la caché de módulos**: envolvió una copia que nadie usaba | ⭐ **su contador dijo «interceptado 0 veces»** |
| 4 | dos pruebas de entrada vacía | pasé un `Map` donde iba una función, y apunté a una arista inexistente | los `TypeError` no eran del proyecto, eran míos |

⇒ **Los tres primeros se cazaron porque el instrumento llevaba dentro un
contador o un control.** El nº3 es exactamente el nº117 (*cinco de diez
mutaciones no llegaron a ocurrir*) repetido por mí, hoy, sabiéndomelo.

**Todo instrumento de este bloque lleva su control positivo Y negativo impreso.**
Cuando abajo aparece un cero, hay un positivo que lo acompaña.

---

# ⛔⛔ HALLAZGOS VIVOS

## V1 · `acera-equivocada.js` sigue midiendo el universo inflado por el centinela

**Dónde:** `src/acera-equivocada.js` (script vivo, la batería lo ejecuta en cada
`--todo` y sale en **código 0**).

La tanda 35 apagó el centinela `99999` **en `direccion.construirIndice`**, que
trabaja sobre una **copia**. `P.cargarPortales()` sigue devolviendo 117 portales
con `n = 99999`, y `acera-equivocada.js` los lee crudos y calcula paridad con
`o.n % 2` — 99999 es impar.

**Medido** interceptando `cargarPortales` en memoria (⛔ sin tocar un fichero) y
comparando las dos salidas enteras:

| lo que imprime hoy | con el centinela apagado |
|---:|---:|
| números que se pueden pedir **150.947** | **50.986** |
| …que son hueco **123.132** | **23.172** |
| …que cambian de acera **66.973** | **16.993** |
| desplazamiento mediano **126 m** | **73 m** |
| `CORRECTO` 9.979 | 9.966 |
| vías con desfase mediano > 50 m: 304 | 303 |

⇒ **Son exactamente los cuatro números que la tanda 35 declaró contaminados y
republicó.** El informe se corrigió; **el script que los produce, no**. Cualquiera
que lo ejecute hoy —incluida la batería, cada vez— vuelve a imprimirlos.

⭐ Lo que **no** se mueve: **los 76 portales enganchados a la acera contraria**
siguen siendo 76. Cambia su denominador (10.055 → 10.042), no el número.

**Qué se apoya en ello:** `docs/H1-ACERA-EQUIVOCADA.md` y `docs/H1-PARIDAD.md`
(registro histórico, ya corregidos por `docs/H1-TOPE-ADELANTO.md`), y la salida en
pantalla de un script que se ejecuta a diario.

**Propuesta (sin aplicar):** decidir dónde vive el apagado del centinela. O
`cargarPortales` lo aplica y `construirIndice` deja de copiar, o `acera-equivocada`
pasa por el índice. ⛔ **Las dos cosas cambian números publicados**, así que es
decisión, no trámite.

---

## ⛔⛔ V2 · DOS MEDIDAS DEL MISMO UNIVERSO QUE DIVERGEN HOY

**Ésta va destacada por la costura del encargo.** Aun apagando el centinela, los
dos caminos **no dan lo mismo**:

| | `acera-equivocada.js` | `medir-paridad.barrer` | diferencia |
|---|---:|---:|---:|
| pedibles | 50.986 | **51.065** | 79 |
| huecos | 23.172 | **23.184** | 12 |
| cambian de acera | 16.993 | **16.981** | −12 |

**La causa está medida, no supuesta:** `acera-equivocada.js` agrupa los portales
por **`codigoVia`** (línea 148); `direccion.construirIndice` agrupa por **núcleo de
vía**, y el propio `medir-paridad.js` ya publica que **63 casillas del índice
mezclan más de una vía** (`CALLE SAN VALERO` + `POLÍGONO SAN VALERO`).

⇒ **Dos universos distintos con el mismo nombre —«las vías con dos hilos»— y el
mismo rótulo impreso —«números que se pueden pedir»—.** El congelado
`buscador.pedibles = 51.065` sólo vigila uno de los dos.

**Propuesta (sin aplicar):** declarar cuál de las dos agrupaciones es *el*
universo y que la otra la llame, o —si las dos son legítimas— **que cada una diga
en su rótulo por qué agrupa**. Hoy un lector no puede saber que son dos cosas.

---

## ⛔⛔ V3 · El titular de `H1-PORTALES.md` está infravalorado por el centinela

**Dónde:** `src/informe-portales.js:166` (`o.n % 2` sobre portales crudos).

```
                                   hoy      limpio
   línea base buena (lado a cara o cruz)   4,5 %      3,6 %
   ⇒ el número publicado                  16,9×      21,3× el azar
```

`⇒ 16,9× el azar` está publicado en **`docs/H1-PORTALES.md` §323**. Con el
centinela apagado son **21,3×**. También se mueven `aristas con ≥4 portales
evaluables` (3.863 → 3.855) y `aristas donde el lado sí se puede usar` (1.735 →
1.731).

⭐ La dirección es conservadora —el método sale **mejor** limpio, no peor—, pero
**el número publicado no es el que sale del dato**. Y la tanda 35 no lo detectó:
su republicación cubrió el universo del buscador, no éste.

---

## ⛔⛔ V4 · El README dice que todavía no hay código

**Dónde:** `README.md`, primer bloque, en cita destacada:

> ## ⚠️ Fase actual: reconocimiento de fuentes. **Todavía no hay código.**
> […] **Ni una línea de aplicación.** Si has llegado buscando una app que
> funcione, todavía no existe.

**Hoy hay 24.931 líneas de JavaScript en `src/`, 70 ficheros**, un grafo de 68.649
nodos y 98.774 aristas, y siete rutas calibradas. Y dos frases más:

- *«todas sus entradas son de datos y de instrumentos que mintieron, **porque
  todavía no hay código que pueda fallar**»* — de las 144 entradas de bitácora, la
  gran mayoría son de código.
- *«**hoy este repositorio no contiene ningún dato integrado**»* — el motor consume
  46.150 portales del callejero municipal y el crudo de OSM.

⇒ **Es la portada pública de un portafolio público, y es el documento más
desactualizado del repositorio.** Un extraño lee esto primero.

**Propuesta (sin aplicar):** reescribir la cabecera y la sección de licencia de
datos. ⚠️ El README **no** es registro histórico: es la puerta. Aquí sí se
reescribe.

---

# ⛔ HALLAZGOS LATENTES

## L1 · El apagado del centinela vive en el sitio equivocado

`portales.js` declara *«la definición vive AQUÍ, una sola vez»* (línea 103), pero
**`cargarPortales()` no la aplica**: lo hace `construirIndice`, sobre una copia.
**29 ficheros requieren `portales.js`.** Es la causa raíz de V1 y V3, y la
próxima vez morderá a quien escriba el siguiente consumidor.

## L2 · `CENTINELA = 9999` y el centinela es `99999`

`src/portales.js:106`. El nombre dice «el valor centinela»; el valor es **un
listón por debajo de él**. Toda la documentación —y el título del propio bloque de
comentario, tres líneas más arriba— dice **99999**.

**Hoy no muerde, y está medido:** cero portales del callejero caen en la franja
(9.999, 99.999), el más alto que pasa el filtro está muy por debajo. ⚠️ Pero
`A.exige(maxN < P.CENTINELA)` está comparando contra 9.999 mientras el informe
dice que vigila el 99.999, y quien escriba `n === P.CENTINELA` se equivocará.

## L3 · `auditoria-paradas.js` no lo ejecuta nadie — y es el instrumento de A2

`src/auditoria-paradas.js`, 96 líneas. **Ni lo requiere ningún fichero, ni lo
ejecuta la batería**: está en la lista `MODULOS` de `probar-paradas.js`, que
declara *«los que NO son ejecutables (módulos puros)»*. **No exporta nada.** Es un
ejecutable metido en la lista de módulos — el mismo animal que el nº131.

⭐ Y lo que mide es exactamente la pregunta A2 de esta auditoría: *qué scripts
imprimen `⛔` y salen en 0*. Lleva 23 tandas sin correr.

## L4 · El veredicto de `orden-numeros.js` son cinco cadenas literales

`src/orden-numeros.js:720-726`, sección «LO QUE NO HACE» — la que un humano lee:

```js
di('   ⛔ …y a IGUAL desorden previo de la calle', '×1,0 en la banda que lleva toda la señal');
di('   ⛔ fallo correlacionado (3 juntos, 200 m)', '0,0 % — ciego por aritmética');
di('   ⛔ vía entera desplazada 200 m',            '0 de 300 vías — no ve nada');
di('   ⛔ los 23 imputables de la tanda 14',       '0 de 7 evaluables');
di('   ⛔ Avenida de la Ilustración',              'MUDO: 22 números para 1.469 portales');
```

Sólo la primera fila de la sección (`×${tasaM/tasaB}`) está calculada. **El script
corre los controles más arriba y luego los re-declara como texto fijo.** Si un
control cambiara de resultado, el resumen seguiría diciendo lo mismo. Es el nº142
—*la cifra que entra en una decisión es la del texto*— dentro del código.

## L5 · `transitabilidad.js` copia la regla y sólo la última fila la llama

`src/transitabilidad.js:68` declara `MU = ['construction', 'proposed', …]`,
**copia carácter a carácter de `planarizar.ESTADOS_MUERTOS`**, y con ella
reimplementa G1, G2 y G3. La fila final —«⇒ LA REGLA»— sí llama a
`transitableAPie`, la función de verdad.

⇒ Si la regla real cambiara, **el total seguiría siendo correcto y la
descomposición que publica se quedaría desfasada**, sin que nada saltara. Hoy los
dos textos son idénticos: **no divergen**.

## L6 · Una vía vacía se declara «de un solo lado»

`Par.analizar([])` y `Par.formaDeVia([])` devuelven **`'un-solo-lado'`**, con
`aplicaParidad: false`. Una lista **vacía** sale con la misma forma que un dato
bueno y con una etiqueta afirmativa, y el aviso al usuario llegaría a decir *«esta
vía solo tiene números pares»* sobre una vía sin ni un portal.

## L7 · El índice se queda a cero sin decir nada

`D.construirIndice(46.150 portales, mapa de vías VACÍO)` → **0 casillas**, sin un
aviso. El buscador contestaría `sin-calle` a todo el mundo y ningún guardián
distinguiría eso de «no está esa calle».

## L8 · `Gr.rutaEntre` sobre un grafo vacío revienta sin nombrar la causa

`TypeError: Cannot read properties of undefined (reading 'map')`. ⭐ El contraste
está al lado: `Mo.construirModelo` con el mismo grafo vacío lanza **`Error:
construirModelo: el grafo no está construido`**, que es la forma correcta y ya
está escrita en el proyecto.

## L9 · 18 commits públicos con el correo personal

De **166 commits**, **148** van con `278133158+ablanquez@users.noreply.github.com`
y **18** con `ablanquez@gmail.com`. Son los **18 más antiguos** (2026-08-02 y
2026-08-03) — exactamente la ley de 001: *el primer commit público hereda la
identidad que tenías puesta*. La ley se aprendió; los 18 primeros ya habían salido.

⚠️ **No se puede deshacer sin reescribir el historial de un repositorio público.**
Va anotado para que la decisión sea explícita.

✅ **Y lo que sí está limpio en todo el historial:** cero `Co-Authored-By`, cero
firmas de herramienta, `data/fuentes/` nunca commiteado, ningún `.env`/`.pem`/
`.key` jamás, y **ni un solo fichero borrado del historial**.

---

# ⚠️ DEUDA

| # | qué | dónde |
|---|---|---|
| **D1** | **`reparto()` implementado 4 veces**, con la misma convención de cuantil. `medir-paridad` **lo exporta** y `medir-listones` define el suyo, byte a byte igual. **Hoy no divergen.** | `acera-equivocada:125` · `enganche:111` · `medir-listones:48` · `medir-paridad:61` |
| **D2** | **Siete listas duplicadas**: `URBANAS` (2, y una está exportada), `ES_EJE`/`SOLO_EJE` (2), `NO_NECESITA`/`NO`/`NO_NECESITAN` (**3**), `FRACCIONES` (2), `WAYS_RUTA7`/`WAYS` (2), `ESTADOS_MUERTOS`/`MU` (2), `FOOTWAY_SIN_NOMBRE` (2, ésta a propósito: es el mutador). **Todas idénticas hoy.** | ver §A3 |
| **D3** | **41 de 64 umbrales de nivel superior no tienen ni un comentario.** `SEMILLA = 20260803` repetido en 7 ficheros, `RADIO_COBERTURA = 60` en 5, `FIRMA = 10` en 3. | ver §A4 |
| **D4** | **Dos radios de enganche.** Los 46.150 portales se enganchan con **120 m** (`portales.engancharUno(maxRadio = 120)`, un defecto sin comentario); una coordenada suelta, con **350 m** (`ruta.MAX_ENGANCHE_M`). Dos definiciones de «suficientemente cerca de la calle» en el mismo motor. | `portales:181` · `ruta:150` |
| **D5** | **`src/portales.js:38-39` son rutas absolutas a otro disco** (`E:/PROYECTOS WEB/01 ZGZ RADAR REACT/…`). Está declarado como decisión («los portales se leen DONDE ESTÁN»), pero **un clon del repositorio público no puede ejecutar ni un script**. | `portales:38` |
| **D6** | Los cuatro visores cargan Leaflet desde **`unpkg.com`**: sin red no abren, y el CDN ve la visita. | `tools/visor-*.html` |
| **D7** | **79 parámetros con valor por defecto en 35 ficheros.** El caso que ya mordió (nº105) está cerrado —`ruta.construir` pasa la zona explícita y hay guardián—, pero el patrón sigue vivo. | ver §A6.2 |

---

# NOTAS

- **N1 · `.gitignore`: correcto, y probado.** 14 rutas comprobadas con
  `git check-ignore` (`.env`, `*.pem`, `id_rsa`, `data/fuentes/`, `node_modules/`,
  los cuatro derivados de `tools/`, `.vscode/`, `*.log`, `*.bak`) **más 6
  positivos de control** que deben entrar y entran. Cero ficheros sin rastrear y
  sin ignorar; cero ficheros versionados que hoy estarían ignorados.
- **N2 · Una cifra de comentario que SÍ reproduce.** El aviso al usuario de
  `ruta.js:184` dice *«el 99 % del callejero está a menos de 65 m»*. Medido:
  **p99 = 55,8 m**, 99,26 % de los portales enganchados por debajo de 65 m. ✅
  Cierto — y **sin ningún guardián** que lo sostenga.
- **N3** · `src/probar-modelo-obligatorio.js:61` cita `C:\Users\…` y `ORDENA~1`
  **dentro de un comentario didáctico** (explica el fallo nº106). No es una ruta
  operativa, pero nombra el directorio del usuario.
- **N4** · Los visores atribuyen `© OpenStreetMap` — sin `contributors` ni enlace
  a la licencia.
- **N5 · ODbL.** El README dice que la licencia de los datos *«se declarará cuando
  se integre alguno»* y que *«el grafo que salga de ahí nacerá bajo esa
  licencia»*. **Ese grafo ya existe.** La declaración está vencida. (Los
  derivados —`tools/*-visor.js`— están gitignoreados, así que hoy no se
  redistribuye base derivada; el día que se publique el visor, sí.)
- **N6** · `data/` versionado contiene sólo `data/pruebas/RUTAS-CONOCIDAS.md` y
  los 35 crudos declarados de `data/exploracion/`. Nada de `E:\` copiado dentro.

---

# A1 · EL CENSO

**74 ficheros `.js`** (70 en `src/`, 4 derivados en `tools/`), **24.931 líneas en
`src/`**. Grafo de `require` construido con un lexer **verificado con 7 casos**
(comentarios, cadenas, plantillas) y con controles positivos **y negativos** de
grafo.

| clase | n | qué significa |
|---|---:|---|
| **librería pura** | 11 | los requiere alguien y la batería NO los ejecuta: `alarma` `condicionales` `direccion` `enganche` `geo` `grafo` `limite` `osm` `planarizar` `portales` `rios` |
| **ejecutable** | 36 | la batería los ejecuta; nadie los requiere |
| **ambos** | 22 | exportan **y** se ejecutan |
| ⛔ **suelto** | **1** | `auditoria-paradas.js` — ver **L3** |
| derivados | 4 | `tools/*-visor.js`, gitignoreados, los regeneran los exportadores |

**Ninguno está MUERTO en el sentido de «bórralo»:** los 36 ejecutables producen
informes publicados y son la trazabilidad de cómo se midió cada cosa. El único
candidato real es `auditoria-paradas.js`, y **no** por muerto: por **no
ejecutado**.

⚠️ **La batería ejecuta 57 de 70.** Excluye 11 librerías (correcto, todas exportan
y las requieren de 2 a 37 ficheros), a sí misma (correcto) y a
`auditoria-paradas.js` (**L3**).

**`NO CONSTA`** de un caso: ninguno. Todos los ficheros quedan clasificados por
`require` resuelto o por ejecución.

---

# A2 · LAS LÍNEAS QUE IMPRIMEN `⛔`/`✅`

**Clasificadas antes de contarse** (ley 29), sobre el código **sin comentarios**
(el lexer tiene su control: un `⛔` de comentario no entra).

| | n |
|---|---:|
| líneas de código que imprimen `⛔`/`✅`/`⚠️` | **764** |
| ⭐ con un `A.exige`/`process.exit` en su vecindad | **217** |
| ⚠️ **sin nada que las vigile** | **547** |
| · contador informativo (interpola un valor calculado) | 124 |
| · prosa larga dentro de una cadena | 377 |
| ⛔ **afirma algo, no interpola nada y nadie la vigila** | **46** |
| `A.exige(` en todo `src/` | **257** |
| `A.fallo(` / `A.imposible(` | 21 |

⚠️ **No he podido reproducir el «182» del encargo**, y digo por qué: la tanda 29
censó **198 comprobaciones** con otra definición (comprobaciones, no líneas
impresas). Éste es un recuento distinto sobre la misma pregunta, no una
corrección de aquél.

**A2.2 — de las 46, las que afirman algo que sostiene una decisión:** leídas una a
una, **37 son títulos de sección o prosa**. Las que quedan y merecen mirada son
las cinco de **L4** (`orden-numeros.js`) más `es-puerta.js:214`, que resultó ser
correcta (está dentro de una rama condicional, no es un literal suelto).

**A2.3 — propuesta (sin aplicar):**

- las **377 de prosa** → dejarlas. No pretenden ser comprobaciones. El coste de
  tocarlas es mayor que el riesgo.
- los **124 contadores** → **degradar el símbolo explícitamente**: que un contador
  informativo no use `⛔`/`✅` sino otra marca. Hoy un lector no distingue
  «esto está mal» de «esto vale 0».
- las **5 de L4** → o se calculan, o se borran del resumen.

## ⭐⭐⭐ A2 EN EJECUCIÓN — el número de verdad

Lo estático de arriba es una criba. **El veredicto sale de ejecutar**, y lo da
`auditoria-paradas.js` — el instrumento de **L3**, el que llevaba 23 tandas sin
correr. Lanzado entero (sin `--rapido`), **58 scripts, 0 saltados**:

| | scripts | líneas `⛔` que imprimen |
|---|---:|---:|
| ⚠️ **imprimen `⛔` y SALEN EN 0** | **37** | **297** |
| ✅ imprimen `⛔` y **no** salen en 0 | 5 | 56 |
| · no imprimen ninguna | 16 | 0 |

> ⛔⛔ **297 líneas `⛔` se imprimen hoy en procesos que terminan en verde.**

⚠️ **Y esto NO significa «297 fallos ocultos»** — el propio instrumento lo avisa en
su cabecera: el símbolo `⛔` se usa igual para declarar un fallo que para escribir
prosa (*«⛔ NO se copian los portales»*) o rotular un control negativo (*«⛔ rodeos
imposibles: 0 ✅»*). **Lo que significa es que 37 scripts no se pueden leer por su
código de salida**, y que la única forma de saber si uno de esos 297 es un fallo
vivo es leerlos.

Los cinco que sí paran son los rojos declarados de siempre:
`auditoria-guardianes` · `donde-falta` · `modelo-rutas` · `pasos` · `rutas-antonio`.

⭐ **El cruce con lo estático es lo que más dice:** 257 `A.exige` frente a 297
líneas `⛔` impresas sin consecuencia. **Hay más `⛔` que no paran nada que
guardianes que paran algo.**

---

# A3 · LO QUE ESTÁ DUPLICADO

**A3.1 — reglas implementadas más de una vez:**

| la regla | copias | ¿divergen hoy? |
|---|---|---|
| **la paridad** (`% 2`, hilos, listón de hilo) | **4**: `paridad.js` (la casa) · `acera-equivocada.js` (`par()`, `hilos()`, su propio `MIN_HILO = 5`) · `informe-portales.js:166` · `orden.js:100` | ⛔⛔ **SÍ — V1, V2 y V3** |
| **el universo «vías con dos hilos»** | **2**: por `codigoVia` · por núcleo de vía | ⛔⛔ **SÍ — V2** |
| **percentiles** (`reparto`) | **4** | ✅ no |
| **G1/G2/G3 de transitabilidad** | **2** | ✅ no (**L5**) |
| **distancia euclídea** | 2: `geo.dist` (arrays) · `paridad.dist` (`.m`) | ✅ no — firmas distintas |

**A3.2 — la pregunta que decide, contestada sobre el dato entero:** la única
divergencia real está en **la paridad**, y es la de los tres hallazgos vivos. Las
demás duplicaciones **dan lo mismo hoy** y son deuda, no fallo.

**A3.3 — datos duplicados:** en §D2. Ninguno diverge hoy.

---

# A4 · LAS CIFRAS QUE VIVEN EN COMENTARIOS

**330 líneas de comentario con una cifra con unidad**, en **50 de 74 ficheros**.
Los que más: `paridad.js` (36), `modelo.js` (24), `asignar-bici.js` (19).

**A4.2 — de las que se pueden comprobar:**

| | |
|---|---|
| ⛔ **NO REPRODUCE** | **1 conocida**: la del nº144 (`paridad.js`, p90 48 · p95 82 frente a 52 · 91), **cerrada ayer** — y su arreglo es hoy el guardián del reparto |
| ✅ **REPRODUCE** | la del aviso al usuario de `ruta.js` (**N2**), comprobada aquí |
| ⚠️ **NO SE PUEDE COMPROBAR** | **las 328 restantes**, y el motivo es el mismo para todas: **son la fotografía de una medida de una tanda concreta, y el código de hoy no vuelve a hacer esa medida.** Comprobarlas una a una es reconstruir 36 tandas |

**A4.3 — ¿sostienen decisiones tomadas?** De los **64 umbrales de nivel superior**,
**17 tienen una cifra en su justificación** y **41 no tienen ningún comentario**.
Los tres del buscador (`RAZONABLE_M`, `ENFRENTE_M`, `ADELANTO_M`) sí sostienen
decisiones y **sí están vigilados desde ayer**. Los 41 sin comentario **no
sostienen nada escrito** — que es otra forma del mismo problema: no se puede
comprobar una justificación que no existe.

⚠️ **Esto es lo más incompleto del bloque, y va dicho:** «no reproduce: 1» **no
significa que sólo haya una mala.** Significa que sólo he podido comprobar dos.

---

# A5 · HIGIENE

**A5.1 · Dato sensible.** Barrido de los **157 ficheros versionados** (14,3 MB, 0
binarios) con 9 patrones y **3 positivos de control** que aciertan.

| patrón | resultado |
|---|---|
| claves privadas (`-----BEGIN … PRIVATE KEY-----`) | ✅ **0** |
| rutas `/home/` o `/Users/` | ✅ **0** |
| rutas absolutas de Windows | **7**, en 5 ficheros: **2 operativas** (`portales.js`, **D5**), 1 didáctica (**N3**), 4 ya generalizadas con `…` en `docs/` |
| correos | **2**: `sig@zaragoza.es` (contacto público en el WFS del Ayuntamiento) y el `noreply` de GitHub en el estado |
| IPs, teléfonos, «cadenas largas» | ⚠️ **todos falsos positivos, comprobados**: identificadores de nodo de OSM en los crudos, nombres de capa del GeoServer, nombres de fichero en el `.gitignore`, y el teléfono público del Ayuntamiento en su propio `GetCapabilities` |

⇒ **Ningún secreto, ninguna credencial, ningún dato personal de terceros.** Lo que
hay es **L9** (el correo del propio autor, en 18 commits) y **D5** (una ruta local
en código).

> ⚠️ **He seguido auditando, y lo digo explícitamente.** La costura decía «para y
> avisa». Juzgué que ninguno de los dos es el caso que esa costura protege —no hay
> nada que contener: ni credencial, ni dato de terceros, ni fuga nueva; los dos son
> visibles desde el commit 1 y uno de ellos es una decisión declarada—. **Si
> Antonio lo lee de otra manera, la auditoría se detiene en este punto y todo lo
> de abajo queda provisional.**

**A5.2 · `.gitignore`:** ✅ correcto y **probado**, no leído. Ver **N1**.

**A5.3 · Identidad:** ver **L9** y el ✅ que lo acompaña.

**A5.4 · README / LICENSE / `CLAUDE.md`:**

- `README.md` → **V4**.
- `LICENSE` → Apache 2.0, 202 líneas, íntegra. ✅ Coherente con lo que el README
  dice del código.
- `CLAUDE.md` → **dice la verdad de lo que hay hoy**, comprobado punto por punto:
  el hook existe (`.githooks/commit-msg`, 8.609 bytes, ejecutable) y hace lo que
  el fichero promete (rechaza `fix:` sin entrada y autogenera el esqueleto);
  `DESPLAZAME-ESTADO.md` no lo ha tocado el ejecutor; `docs/RECONOCIMIENTO-*` e
  `INVENTARIO-*` no se han reescrito. La única excepción declarada —la
  generalización de rutas locales— **está cumplida en `docs/` y no en `src/`**
  (D5), y el propio `CLAUDE.md` dice «en cada fichero afectado» sin acotar a
  `docs/`.

**A5.5 · Terceros:** ver **N4**, **N5**, **N6**.

---

# A6 · EL CÓDIGO DE PRODUCCIÓN

**A6.1 · La cadena, paso a paso.** `dirección → portal → enganche → grafo →
Dijkstra → tramos → texto`. Puntos donde se pierde información o se decide sin
declararlo:

1. **dirección → portal.** Declarado y con estados propios (`exacto`,
   `numero-aproximado`, `sin-numero-cerca`…). ⛔ Aquí está **V2**: qué es «una
   vía» depende de quién pregunte.
2. **portal → enganche.** ⚠️ **Dos radios distintos** (**D4**), y **124 de 46.150
   portales no se enganchan** (0,3 %) por quedar a más de 120 m. El aviso al
   usuario existe y su cifra reproduce (**N2**).
3. **grafo → Dijkstra.** ✅ El coste es **la longitud en metros**, declarado en la
   primera línea de `grafo.js`. `adyacencia(…, soloAPie = true, sinCondicionales =
   false)`: los dos defectos están escritos y **el único llamador pasa los dos
   valores explícitos**. Los pasos condicionales entran a propósito (decisión de
   Antonio, marcada y contada).
4. **Dijkstra → tramos → texto.** ✅ El total sale de `r.dist`, **no** de sumar
   `e.largo` — y está escrito por qué (las aristas de los extremos van partidas).
   ⚠️ `Rel.tramoDeArista` sobre una arista vacía devuelve un tramo bien formado
   con `metros: 0` (**relacionado con L6-L7**).

**A6.2 · Los defectos:** **79 parámetros con valor por defecto en 35 ficheros**;
la lista completa está en el volcado del instrumento. Los que de verdad se usan
sin pasar valor son los de carga (`cargarPortales(ruta = RUTA_PORTALES)`,
`cargarVias`, `cargar(ruta = CRUDO_RIOS)`) y los de indexado (`celda = 100`,
`celda = 200`, `celda = 20`). ⭐ **El que costó el nº105 está cerrado**: `construir`
recibe zona explícita y hay guardián (`probar-guardianes.js`).

**A6.3 · `try/catch`:** **39 en total, y sólo 8 fuera de scripts de prueba.** Los
de producción están **declarados y son correctos**: el de `ruta.js:361` es el
`catch` de nivel CLI que **sale en 1**, y el comentario de al lado explica que
antes había ahí un «avisa y sigue» que dejó el motor sin modelo dos tandas.
`modelo.js` **no** usa `try/catch` para eso: comprueba y lanza con mensaje propio.
✅ **Este apartado sale limpio, y esta vez tiene explicación:** es la clase de
fallo que el proyecto ya pagó (nº105) y arregló nombrándola.

**A6.4 · Entradas vacías —probadas, no razonadas:**

| entrada vacía | qué hace |
|---|---|
| `construirIndice([], Map vacío)` | ⚠️ 0 casillas, en silencio |
| `construirIndice(46.150 portales, Map de vías VACÍO)` | ⚠️ **0 casillas, en silencio** (**L7**) |
| `resolver("Calle X 5", índice vacío)` | ⚠️ `sin-calle` — indistinguible de «no existe esa calle» |
| `resolver("", índice real)` | ⚠️ `sin-calle` |
| `Par.decidir(5, [], …)` | ✅ `sin-numeros`, con aviso propio |
| `Par.analizar([])` / `formaDeVia([])` | ⛔ **`un-solo-lado`** (**L6**) |
| `Par.cota(5, [])` · `masCercanoEnNumero([], 5)` | ✅ `null` |
| `Par.deEnfrente(5, [], null)` | ✅ `[]` |
| `Mo.construirModelo(grafo vacío, [])` | ✅ **`Error` con mensaje propio** |
| `Gr.rutaEntre(grafo vacío)` | ⛔ `TypeError` (**L8**) |
| `Rel.tramoDeArista(arista sin puntos)` | ⚠️ tramo bien formado, `metros: 0` |

---

# ⚠️ Z · LO QUE NO SE HA PODIDO AUDITAR

⛔ `NO CONSTA` aquí significa **no se puede saber con lo hecho**, no «no lo he
mirado». Cada línea dice cuál de las dos es.

| qué | por qué |
|---|---|
| **Cuáles de las 297 `⛔` impresas son un fallo vivo** | **no lo he mirado, y son 297.** El recuento está hecho (§A2 en ejecución) y el volcado con las líneas por script existe; clasificar una a una es el trabajo que esto deja pendiente. ⚠️ Y es el sitio exacto donde estuvo dos tandas la ruta rota del casco. |
| **328 de las 330 cifras de comentario** | **NO CONSTA, y es estructural**: son fotografías de medidas de tandas concretas que el código de hoy ya no repite. Comprobarlas exige reconstruir el estado de cada tanda. |
| **Si las duplicaciones que hoy coinciden coincidieron siempre** | **NO CONSTA.** He comparado el estado de HOY sobre el dato entero. Que `reparto()` diera lo mismo en la tanda 12 no lo sé. |
| **Si los 36 ejecutables sostienen de verdad lo que dicen sostener** | **no lo he mirado**, y lo digo así. He clasificado por `require` y por ejecución, no he cruzado cada script con cada número de `docs/`. Son 40 informes. |
| **Si `docs/` dice la verdad** | **fuera de alcance de este bloque.** V1 y V3 son un aviso de que puede no decirla en más sitios: los dos salieron de ejecutar el código, no de leer los informes. |
| **El contenido de los 4 derivados de `tools/`** | no versionados y regenerables; no los he inspeccionado. |
| **Si el motor calcula bien** | ⛔ **este bloque no lo audita.** He auditado que la cadena no pierda información y que las entradas raras no pasen por buenas. Que la ruta que sale sea la ruta correcta lo sostienen las siete rutas y `RUTAS-CONOCIDAS.md`, y eso es otro bloque. |

---

## ⚠️ Y una advertencia sobre este bloque

**No ha salido limpio, pero tampoco ha salido completo.** Los cuatro hallazgos
vivos salieron de **ejecutar** cosas —interceptar `cargarPortales`, comparar dos
salidas enteras, ejecutar el auditor dormido, leer el README— y **ninguno de los
cuatro lo habría encontrado un barrido estático**. ⇒ la parte estática de esta
auditoría es la más floja, y hay que leerla sabiéndolo.

⚠️ Y una cosa más, que es el resumen honesto del bloque: **de los cinco hallazgos
principales, tres los encontró un instrumento que ya existía en el repositorio y
que nadie ejecutaba** (`auditoria-paradas.js`) o una comparación que nadie había
hecho (el mismo script con y sin centinela). **No hacía falta escribir código nuevo
para encontrarlos: hacía falta correr el que había.**

⭐ Lo que sí se puede afirmar con lo medido: **la clase de fallo dominante en este
repositorio no es el código roto — es la medida que se corrigió en el informe y no
en el instrumento.** V1, V3 y el nº144 son el mismo animal tres veces.
